<%= form_for(@survey_answer) do |f| %>
  <% if @survey_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@survey_answer.errors.count, "error") %> prohibited this survey_answer from being saved:</h2>

      <ul>
      <% @survey_answer.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :survey %><br>
    <%= f.text_field :survey %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
